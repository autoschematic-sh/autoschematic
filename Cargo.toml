[workspace]

members = [
    "autoschematic",
    "autoschematic-server",
    "autoschematic-core",
    "autoschematic-lsp",
    #"connectors/autoschematic-connector-python-host",
]

[profile.release]
debug = 1
