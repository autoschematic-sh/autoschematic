[workspace]

members = [
    "autoschematic",
    "autoschematic-server",
    "autoschematic-core",
    "autoschematic-lsp",
    "autoschematic-macros",
]

[profile.release]
codegen-units = 1
lto = true
strip = "symbols"
