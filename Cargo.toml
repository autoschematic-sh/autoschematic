[workspace]

members = [
    "autoschematic",
    "autoschematic-core",
    "autoschematic-cli",
    "autoschematic-lsp",
    "connectors/autoschematic-connector-python-host",
]

[profile.release]
debug = 1
